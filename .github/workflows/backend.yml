name: Money With History PHP

on: [workflow_dispatch, push, pull_request]

permissions:
  contents: read

jobs:
  run:
    uses: flarum/framework/.github/workflows/REUSABLE_backend.yml@2.x
    with:
      enable_backend_testing: true

      backend_directory: .
